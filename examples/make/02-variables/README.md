# 示例 02: 变量和自动化变量

演示 Makefile 中变量和自动化变量的使用。

## 编译运行

```bash
make        # 观察输出，可以看到自动化变量的值
./main      # 运行程序
make clean  # 清理
```

## 关键点

1. `CC`、`CFLAGS`、`TARGET` 是用户定义的变量
2. `$@` 表示目标名
3. `$^` 表示所有依赖
4. `$<` 表示第一个依赖
5. `@echo` 中的 `@` 防止命令本身被打印
